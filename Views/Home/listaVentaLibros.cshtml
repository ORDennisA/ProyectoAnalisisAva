
<li class="nav-item">
    <a href="@Url.Action("FormularioVentaLibros","Home")" class="btn btn-primary">Agregar ventas de libros</a>
</li>
<li class="nav-item">
    <a href="@Url.Action("EditarVentaLibros","Home")" class="btn btn-danger">Borrar/Editar</a>
</li>

<div class="col-sm-8"><h2>Ventas de libros </h2></div>
<table class="table table-condensed">
    <tr>
        <th>id_Libro</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td> @item.id_Libro</td>
        </tr>

    }
</table>